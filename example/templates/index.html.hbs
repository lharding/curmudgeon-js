{{> global_head}}

<div class="posts_container">
	{{#posts}}
		{{> post_frontpage parent=..}}
	{{/posts}}
</div>

<div class="tag_sidebar">
  <ul>
    {{#tags}}
      <li><a href="{{../url_prefix}}{{page}}">{{tag}}</a>
    {{/tags}}
  </ul>
</div>

{{> global_foot}}
